This app assigns the task of manipulating .csv files to a Gemini-based agent in order to train a model on that data.  

What works now:  
-Interface  
-API key saving  
-Chatting with the agent  
-Launcher to load chats or create new ones  
-Saving chats  
  
First-Time Instructions:  
-Use python version 3.10.8  
-Start a new terminal window inside the project  
-run "python3 -m venv venv"  
  
FOR WINDOWS:  
-run "venv/Scripts/activate"  
FOR MAC:  
-run "source venv/bin/activate"  
-run "pip install -r requirements.txt"  
-run "python3 main.py"  
  
Whenever you want to use the app:  
-check if you are on the required python version  
-check if you have activated the virtual environment (venv)  
-run "python3 main.py"  
  
Fixed problems on Mac devices  

To-Do next:  
-small window where the user drops the wanted csv file
-tools for the agent to apply changes to that file
  
I updated the code with a lot of notes for anyone interested.  
Feel free to read the code and add suggestions in the Issues tab!
